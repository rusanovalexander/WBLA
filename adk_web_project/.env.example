# Credit Pack ADK agent — copy to .env and fill in
# See https://google.github.io/adk-docs/agents/models for model / auth options.

# Vertex AI (recommended)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Or Gemini API key (dev)
# GOOGLE_API_KEY=your-api-key

# Model for root agent (optional)
# ADK_CREDIT_PACK_MODEL=gemini-2.0-flash
