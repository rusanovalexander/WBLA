# Model backend: 0 = Gemini API, 1 = Vertex AI
GOOGLE_GENAI_USE_VERTEXAI=1

# Vertex AI (when GOOGLE_GENAI_USE_VERTEXAI=1)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: service account key path
# GOOGLE_APPLICATION_CREDENTIALS=auth_key.json

# Vertex AI Search (Discovery Engine) — required for procedure/guidelines RAG
DATA_STORE_ID=your-data-store-id
SEARCH_LOCATION=global

# Root agent model
ROOT_AGENT_MODEL=gemini-2.5-flash
